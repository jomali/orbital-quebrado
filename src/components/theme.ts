import { createTheme } from "@mui/material/styles";

export default createTheme({
  palette: {
    primary: {
      main: "#DA0037",
    },
    background: {
      default: "#171717",
      paper: "#171717",
    },
    mode: "dark",
  },
});
